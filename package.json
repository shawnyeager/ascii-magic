{
  "name": "ascii-magic",
  "version": "0.1.0",
  "private": true,
  "description": "Agent Plugin that encodes and decodes ASCII Magic editor recipes and deep links. Does not render images.",
  "license": "MIT",
  "type": "commonjs",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "mcp": "node src/server.js",
    "check": "node --check src/server.js && node --check src/recipe.js && node --check scripts/roundtrip.js",
    "test": "node scripts/roundtrip.js"
  }
}
